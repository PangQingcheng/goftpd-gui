{
  "name": "ftp-server",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "pack": "electron-packager . go-ftpd --win --out=./out --arch=x64 --app-version=0.0.1 --overwrite --icon=./ftp-server.ico"
  },
  "devDependencies": {
    "electron": "^9.1.0"
  },
  "dependencies": {
    "jquery": "^3.4.1",
    "tree-kill": "^1.2.1",
    "open": "^6.4.0",
    "ini": "^1.3.5"
  }
}
